export interface IAddRate {
  bookingId: number;
  rating: number;
  comments: string;

  // -----------
  // userID: number;
  // driverID: number;
  // reservationID: number;
  // score: number;
  // review: string;
}
